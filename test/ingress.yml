apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mail
spec:
  rules:
    - host: mail
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mail
                port:
                  number: 8080